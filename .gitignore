# секреты — НИКОГДА в репозиторий
.env
.env.*
!.env.example
.blogger-tokens.json  # ops/blogger-tokens.py: локальные Blogger OAuth-ключи (не коммитить)
vk_secrets.txt          # bootstrap кладёт сюда VK_REFRESH_TOKEN/VK_DEVICE_ID (перенёс в Secrets — удали)
vk_debug.txt            # bootstrap пишет сюда текст ошибки для диагностики (токенов не содержит)

# генерится в CI (fetch_rates.py) — не коммитим
rates.json

# черновики LLM (llm_draft.py) — только локально, публикуются после проверки
drafts/
# сборка и мусор
dist/
build/
__pycache__/
*.pyc
*.spec
.DS_Store
market.json
fng.json
trending.json
yandex.json
metrika.json
